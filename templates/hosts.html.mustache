      <div class="page-header">
        <h1>Hosts</h1>
      </div>

      <div class="row">
        <div class="col-md-12">
          <table class="table table-striped">
            <thead>
              <tr>
                <th>Hostname</th>
              </tr>
            </thead>
            <tbody>
              {{#.}}
              <tr>
                <td><a href="/hosts/{{.}}">{{.}}</a></td>
              </tr>
              {{/.}}
            </tbody>
          </table>
        </div>
      </div>
